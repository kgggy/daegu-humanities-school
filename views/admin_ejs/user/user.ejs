<h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">회원관리 /</span> 일반회원관리</h4>
<!-- Basic Bootstrap Table -->
<div class="card">
    <h5 class="card-header">일반회원관리</h5>
    <div class="table-responsive text-nowrap">
        <form name="searchForm" action="" method="get" class="easydropdown">
            <input type="hidden" name="page" value="1">
        <select name="searchType1" class="dropdown">
            <option value="">기수</option>
            <option value="01기" <%if(searchType1=="01기"){%> selected="selected" <%}%>>01기</option>
            <option value="02기" <%if(searchType1=="02기"){%> selected="selected" <%}%>>02기</option>
            <option value="03기" <%if(searchType1=="03기"){%> selected="selected" <%}%>>03기</option>
            <option value="04기" <%if(searchType1=="04기"){%> selected="selected" <%}%>>04기</option>
            <option value="05기" <%if(searchType1=="05기"){%> selected="selected" <%}%>>05기</option>
            <option value="06기" <%if(searchType1=="06기"){%> selected="selected" <%}%>>06기</option>
        </select>
        <select name="searchType2" class="dropdown">
            <option value="">직책</option>
            <option value="회장" <%if(searchType2=="회장"){%> selected="selected" <%}%>>회장</option>
            <option value="감사" <%if(searchType2=="감사"){%> selected="selected" <%}%>>감사</option>
            <option value="골프사무국장" <%if(searchType2=="골프사무국장"){%> selected="selected" <%}%>>골프사무국장</option>
            <option value="골프회장" <%if(searchType2=="골프회장"){%> selected="selected" <%}%>>골프회장</option>
            <option value="교학처장" <%if(searchType2=="교학처장"){%> selected="selected" <%}%>>교학처장</option>
            <option value="부회장" <%if(searchType2=="부회장"){%> selected="selected" <%}%>>부회장</option>
            <option value="사무처장" <%if(searchType2=="사무처장"){%> selected="selected" <%}%>>사무처장</option>
            <option value="산악사무국장" <%if(searchType2=="산악사무국장"){%> selected="selected" <%}%>>산악사무국장</option>
            <option value="산악회장" <%if(searchType2=="산악회장"){%> selected="selected" <%}%>>산악회장</option>
            <option value="상임부회장" <%if(searchType2=="상임부회장"){%> selected="selected" <%}%>>상임부회장</option>
            <option value="원장" <%if(searchType2=="원장"){%> selected="selected" <%}%>>원장</option>
            <option value="의전국장" <%if(searchType2=="의전국장"){%> selected="selected" <%}%>>의전국장</option>
            <option value="의전차장" <%if(searchType2=="의전차장"){%> selected="selected" <%}%>>의전차장</option>
            <option value="이사" <%if(searchType2=="이사"){%> selected="selected" <%}%>>이사</option>
            <option value="자문" <%if(searchType2=="자문"){%> selected="selected" <%}%>>자문</option>
            <option value="재무국장" <%if(searchType2=="재무국장"){%> selected="selected" <%}%>>재무국장</option>
            <option value="재무차장" <%if(searchType2=="재무차장"){%> selected="selected" <%}%>>재무차장</option>
            <option value="총무국장" <%if(searchType2=="총무국장"){%> selected="selected" <%}%>>총무국장</option>
            <option value="총무차장" <%if(searchType2=="총무차장"){%> selected="selected" <%}%>>총무차장</option>
            <option value="특별위원장" <%if(searchType2=="특별위원장"){%> selected="selected" <%}%>>특별위원장</option>
            <option value="편집국장" <%if(searchType2=="편집국장"){%> selected="selected" <%}%>>편집국장</option>
            <option value="학장" <%if(searchType2=="학장"){%> selected="selected" <%}%>>학장</option>
            <option value="행정실장" <%if(searchType2=="행정실장"){%> selected="selected" <%}%>>행정실장</option>
            <option value="홍보국장" <%if(searchType2=="홍보국장"){%> selected="selected" <%}%>>홍보국장</option>
            <option value="홍보차장" <%if(searchType2=="홍보차장"){%> selected="selected" <%}%>>홍보차장</option>
            <option value="회원" <%if(searchType2=="회원"){%> selected="selected" <%}%>>회원</option>
        </select>
        <select name="searchType3" class="dropdown">
            <option value="">동아리</option>
            <option value="총동창회" <%if(searchType3=="총동창회"){%> selected="selected" <%}%>>총동창회</option>
            <option value="골프회" <%if(searchType3=="골프회"){%> selected="selected" <%}%>>골프회</option>
            <option value="산악회" <%if(searchType3=="산악회"){%> selected="selected" <%}%>>산악회</option>
        </select>
        <% if(searchText != '') { %> <input class="searchbox" type="text" name="searchText"
                                            value="<%=searchText%>" />
                                        <% } else {%>
                                        <input class="searchbox" type="text" name="searchText"
                                            placeholder=" 회원명, 소속명" />
                                        <% } %>
                                        <button class="btn btn-block btn-outline-success" type="submit"
                                            id="searchAction" style="margin-bottom: 10px;">검색</button>
        </form>
        <table class="table table-hove">
            <thead>
                <tr>
                    <th><input type="checkbox" name="chkAll" id="chkAll" /></th>
                    <th>번호</th>
                    <th>이름</th>
                    <th>기수</th>
                    <th>휴대폰</th>
                    <th>직책</th>
                    <th>소속</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                <% if(results.length > 0) { %>
                    <% for(var i = (page * page_num) - page_num; i < (page * page_num); i++) { 
                    if(i > length){
                        i++;
                    } else { %>
                    <tr class="pointer"
                        onclick="location.href='/admin/m_user/selectOne?uid=<%=results[i].uid%>&page=<%=page%><%= keepSearch %>'">
                        <td onclick='event.cancelBubble=true;'><input type="checkbox" id="chk"
                                name="chk" value="<%=results[i].uid %>"
                                data-img="<%=results[i].userImg%>" />
                        </td>
                        <td>
                            <%=results[i].uid%>
                        </td>
                        <td>
                            <i class="fab fa-angular fa-lg text-danger me-3"></i> <strong><%=results[i].userName%></strong>
                        </td>
                        <td>
                            <%=results[i].userNum%>
                        </td>
                        <td>
                            <%= results[i].userPhone %>
                        </td>
                        <td>
                            <%= results[i].userAuth %>
                        </td>
                        <td>
                            <%= results[i].userJob %>
                        </td>
                    </tr>
                    <% } %>
                    <% } %>
                    <% } else { %>
                    <tr>
                        <td colspan="7">등록된 회원이 없습니다.</td>
                    </tr>
                    <% } %>
                    <!-- <td>
                        <div class="dropdown">
                            <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                <i class="bx bx-dots-vertical-rounded"></i>
                            </button>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="javascript:void(0);"><i class="bx bx-edit-alt me-1"></i>
                                    Edit</a>
                                <a class="dropdown-item" href="javascript:void(0);"><i class="bx bx-trash me-1"></i>
                                    Delete</a>
                            </div>
                        </div>
                    </td> -->
            </tbody>
        </table>
        <div>
            <div>
                <button type="button" id="enrollAction"
                    class="btn btn-block btn-outline-success"
                    style="width: 60px; display: inline-block;"
                    onclick="location.href='/admin/m_user/userInsertForm'">등록</button>
                <input type="hidden" id="userImg" name="userImg" value="" />
                <input type="hidden" id="uid" name="uid" value="" />
                <button class="btn btn-block btn-outline-danger" type="button"
                    id="delete_btn">삭제</button>
                <input class="btn btn-block btn-outline-success" type="button"
                    id="excelFileExport" value="엑셀 다운로드"
                    style="margin-bottom: 15px; width: 140px; display: inline-block;" />
            </div>
            <div class="mt_20">
                <div class="paging" id="page_navi">
                    <ul class="pagination customPaging">
                        <li> <a href="/admin/m_user?page=1<%= keepSearch %>" class="num">
                                <<</a> </li> <li> <a
                                        <%if((page<startPage)||(page==1)){%><%} else {%>
                                        href="/admin/m_user?page=<%= parseInt(page) - 1 %><%= keepSearch %>"
                                        class="num" <% } %>>
                                        <</a> </li>
                                            <% for(var i = startPage; i < endPage ; i++){  %>
                                            <li class="paging_on">
                                            <a href="/admin/m_user?page=<%= i + 1 %><%= keepSearch %>"
                                                <%if(page==i+1){%>class="selected"
                                                <%}%> class="num"><%= i + 1 %></a> </li> <% } %>
                                                <li> <a <%if(page>last-1){%><%} else {%>
                                                    href="/admin/m_user?page=<%= parseInt(page) + 1 %><%= keepSearch %>"
                                                    class="num" <% } %>>></a></li>
                        <li><a href="/admin/m_user?page=<%= last %><%= keepSearch %>"
                                class="num">>></a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<!--/ Basic Bootstrap Table -->

<script>
    $(document).ready(function () {
        const formObj = $("form[name='userForm']");
        // 삭제
        $("#delete_btn").on("click", function () {
            var chkArr = [];
            $("input[name=chk]:checked").each(function () {
                chkArr.push($(this).val());
            });
            $('#uid').val(chkArr);
            if (chkArr == "") {
                alert("삭제할 목록을 선택해주세요.");
                return false;
            }
            var chkImg = [];
            $("input[name=chk]:checked").each(function () {
                chkImg.push($(this).attr('data-img'));
            });
            $('#userImg').val(chkImg);
            formObj.attr("action", "userDelete");
            formObj.attr("method", "get");
            if (confirm("삭제하시겠습니까?") == true) {
                formObj.submit();
            } else {
                return false;
            }
        })
        //쿼리스트링 페이지
        function getUrlParams() {
            var params = {};
            window.location.search.replace(/[?&]+([^=&]+)=([^&]*)/gi, function (str, key, value) {
                params[key] = value;
            });
            return params;
        }
        var param = getUrlParams();
        var page = document.getElementById("page");
        page.value = param.page;
        const searchForm = $("form[name='searchForm']");
        //검색
        $("#searchAction").on("click", function () {
            searchForm.attr("action", "/admin/m_user");
            searchForm.attr("method", "get");
            searchForm.submit();
        })
        // 엑셀 다운로드
        $("#excelFileExport").on("click", function () {
            formObj.attr("action", "userExcel");
            formObj.attr("method", "get");
            formObj.submit();
        })


    });

    // 레이아웃 adminNav 해당 탭에 접근했을때 펼쳐주기 '[0] = 회원관리'
    var navDropdown = document.getElementsByClassName("nav-dropdown");
    navDropdown[0].className += 'open';

    // 선택
    $("#chkAll").click(function () {
        if ($("#chkAll").is(":checked"))
            $("input[name=chk]").prop("checked", true);
        else
            $("input[name=chk]").prop("checked", false);
    });
    $("input[name=chk]").click(function () {
        var total = $("input[name=chk]").length;
        var checked = $("input[name=chk]:checked").length;
        if (total != checked)
            $("#chkAll").prop("checked", false);
        else
            $("#chkAll").prop("checked", true);
    });
</script>